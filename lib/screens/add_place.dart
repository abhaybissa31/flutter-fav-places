import 'package:flutter/material.dart';

class AddNewPlaces extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('dummy but working'),
        automaticallyImplyLeading: false,
      ),
    );
  }
}